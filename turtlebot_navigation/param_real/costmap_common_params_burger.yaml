obstacle_range: 3.0 #maximum range sensor reading that will result in an obstacle being put into the costmap
raytrace_range: 3.5 #range to which we will raytrace freespace given a sensor reading

footprint: [[-0.075, -0.075], [-0.075, 0.075], [0.041, 0.075], [0.041, -0.075]]
#robot_radius: 0.075 # 0.105 is actual

# inflation_radius: 0.5
# cost_scaling_factor: 3.0

map_type: costmap
# observation_sources: scan
# scan: {sensor_frame: base_scan, data_type: LaserScan, topic: scan, marking: false, clearing: true}